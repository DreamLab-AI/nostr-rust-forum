name = "nostr-bbs-relay"
main = "build/worker/shim.mjs"
compatibility_date = "2025-09-01"

[build]
command = "worker-build --release"

[vars]
RELAY_NAME = "Nostr BBS Relay"
ADMIN_PUBKEYS = ""
ALLOWED_ORIGIN = "https://example.com"
ALLOWED_ORIGINS = "https://example.com"

[[d1_databases]]
binding = "DB"
database_name = "nostr-bbs-relay"
database_id = "97c77d23-0e24-4325-ada7-1747eab4095b"
migrations_dir = "migrations"

# NIP-98 replay protection now uses the D1 database (nip98_replay table)
# instead of KV. The relay-worker's own DB binding serves double duty.

[durable_objects]
bindings = [
  { name = "RELAY", class_name = "NostrRelayDO" }
]

[[migrations]]
tag = "v1"
new_sqlite_classes = ["NostrRelayDO"]

[triggers]
crons = ["*/5 * * * *"]
